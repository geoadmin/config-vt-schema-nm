tables:
  layer_construct:
    type: polygon
    columns:
    - name: the_geom
      type: geometry
    - name: class
      type: string
    mapping:
      lbm.construct:
        - dam
        - lock
        - platform
  #      - solar_panel